#include<stdio.h>
int main()
{
    int length,breadth,area;
    printf("enter a length");
    scanf("%d",&length);
    printf("enter a breadth);
    scanf("%d",&breadth);
    area=length*breadth;
    printf("area is:%d",area);
}
